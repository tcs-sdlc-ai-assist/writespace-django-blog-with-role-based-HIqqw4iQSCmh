# accounts/models.py
#
# The accounts app uses Django's built-in User model (django.contrib.auth.models.User)
# for authentication and user management. No custom models are required.
#
# This file is present to follow Django app conventions. If proxy models or
# helper functions related to user data are needed in the future, they should
# be added here.